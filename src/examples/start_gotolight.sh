#!/bin/bash

# starts goto_light example using the configuration file 'goto_light.json' and
# the initial positions defined in 'pose_gotolight.json'
./gotolight -p gotolight.json -b pose_gotolight.json

